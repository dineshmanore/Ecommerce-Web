spring:
  application:
    name: ecommerce-backend
  data:
    mongodb:
      uri: mongodb+srv://Dinesh:<db_password>@cluster0.5gaccml.mongodb.net/?appName=Cluster0
      database: ecommerce

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:mySecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLong}
  expiration: 86400000 # 24 hours in milliseconds

cors:
  allowed-origins: "*"

logging:
  level:
    com.ecommerce: DEBUG
    org.springframework.security: DEBUG